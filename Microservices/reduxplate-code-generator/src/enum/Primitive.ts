enum Primitive {
    'number' = 'number',
    'boolean' = 'boolean',
    'string' = 'string',
    'symbol' = 'symbol',
    'bigint' = 'bigint',
    'number[]' = 'number[]',
    'boolean[]' = 'boolean[]',
    'string[]' = 'string[]',
    'symbol[]' = 'symbol[]',
    'bigint[]' = 'bigint[]',
}

export default Primitive